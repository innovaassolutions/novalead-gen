# ============================================
# Vercel / Next.js (.env.local)
# ============================================
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Required for production builds on Vercel (get from Convex dashboard → Settings → Deploy Key)
CONVEX_DEPLOY_KEY=

# ============================================
# Convex Environment Variables (set in Convex dashboard)
# ============================================
# Shared secret between Convex HTTP endpoints and Railway workers
# RAILWAY_WORKER_SECRET=

# ============================================
# Railway Workers (workers/.env)
# ============================================
# CONVEX_HTTP_URL=https://your-deployment.convex.site
# RAILWAY_WORKER_SECRET=          (must match Convex env var above)
# ANTHROPIC_API_KEY=sk-ant-...    (Claude API for enrichment/analysis)
# SERPAPI_KEY=                     (Google Maps / Ads scraping)
# INSTANTLY_API_KEY=               (cold email campaign management)
# NOVACRM_URL=https://novacrm.innovaas.co
# NOVACRM_LEAD_CAPTURE_API_KEY=   (matches NovaCRM's LEAD_CAPTURE_API_KEY)
# LOG_LEVEL=info

# ============================================
# App UI — Integrations Page (stored in Convex settings table)
# ============================================
# These are entered via the dashboard Integrations page, not as env vars:
# - Instantly API Key
# - NovaCRM URL + API Key
